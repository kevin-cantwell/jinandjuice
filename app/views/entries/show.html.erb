<div class="name">
  <%= @entry_form.name %>
</div>
<div class="story">
  <% if @entry_form.message? %>
    <div class="event message">
      <% split = @entry_form.message.split(' ') %>
      <span class="<%= @entry_form.message.length > 430 ? 'first-word' : ''%>"><%= split.first %></span> <%= split.slice(1, split.length).join(' ') %>
    </div>
  <% end %>
  <% if @entry_form.photo? %>
    <div class="event photo">
      <div align="center">
        <span class="frame">
          <img src="<%= @entry_form.photo_url %>" />
        </span>
      </div>
    </div>
  <% end %>
  <% if @entry_form.video? %>

    <div class="event video">
      <div align="center">
        <% if @entry_form.video_ready? %>
          <span class="frame">
            <% if @entry_form.id == 1 %>
              <video class="vjs-tech" id="video-0_html5_api" preload="auto" poster="https://s3.amazonaws.com/jinandjuice/jasonfeng.jpg" src="https://s3.amazonaws.com/jinandjuice/AMYJIN30.m4v" controls></video>
            <% else %>
              <video class="vjs-tech" id="video-0_html5_api" preload="auto" poster="<%= @entry.video_poster %>" src="<%= @entry.video_url %>" controls></video>
            <% end %>
          </span>
        <% else %>
           Your video is processing: <%= @entry_form.video_percent_complete %>%
        <% end %>
      </div>
    </div>
  <% end %>
</div>
<div class="directions">
  <a href="/">Home</a> | <a href="/entries/<%= @entry_form.id %>/edit">Edit</a>
</div>